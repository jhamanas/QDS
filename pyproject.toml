[project]
name = "qds"
version = "0.1.0"
description = "Quantum-inspired digital signature threat detection dashboard"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24,<3"]

[tool.vercel]
entrypoint = "api.run:handler"
